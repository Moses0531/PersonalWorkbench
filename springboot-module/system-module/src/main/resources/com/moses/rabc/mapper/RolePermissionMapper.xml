<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moses.rabc.mapper.RolePermissionMapper">

    <resultMap id="BaseResultMap" type="com.moses.rabc.entity.RolePermission">
            <id property="id" column="id" />
            <result property="roleId" column="role_id" />
            <result property="permissionId" column="permission_id" />
    </resultMap>

    <select id="selectPermissionsByRoleId" resultMap="com.moses.rabc.mapper.SysPermissionMapper.BaseResultMap">
        SELECT p.*
        FROM sys_permission p
        INNER JOIN role_permission rp ON p.permission_id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        ORDER BY p.display_order ASC, p.permission_id ASC
    </select>

    <delete id="deleteByRoleIdAndPermissionId">
        DELETE FROM role_permission
        WHERE role_id = #{roleId}
          AND permission_id = #{permissionId}
    </delete>

</mapper>
